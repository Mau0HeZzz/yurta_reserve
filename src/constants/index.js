import { convertIntegerToText, numberByDigits } from "@/constants/helpers";
import { scrollbarSettings } from "@/constants/scrollbar";
import { mapData } from "@/constants/placeholer";

export {
  convertIntegerToText,
  numberByDigits,
  mapData,
  scrollbarSettings
}
